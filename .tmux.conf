unbind r
bind r source-file ~/.tmux.conf

# remap the prefix 
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# changing the panes
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# don't rename windows automatically => ',' for custom name
set-option -g allow-rename off

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# spliting the windows
bind | split-window -h 
bind - split-window -v 
unbind '"'

# create session in current directory
# bind % new-session -c "#{pane_current_path}

unbind %
# bind % attach-session -c "#{pane_current_path}"
bind % new-window -c "#{pane_current_path}"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'dracula/tmux'

## UI TWEAKS
#
# copy mode
# setw -g mode-style 'fg=colour4 bg=colour18 bold'
# pane borders
# set -g pane-border-style 'fg=colour4'
# set -g pane-active-border-style 'fg=colour111'

# statusbar
set -g status-position bottom
set -g status-justify left
# set -g status-style 'fg=#858bd3'
# set -g status-left ''
# set -g status-right ''
# set -g status-right '%Y-%m-%d %H:%M '
# set -g status-right-length 50
# set -g status-left-length 10

# setw -g window-status-current-style 'fg=colour0 bg=colour111 bold'
# setw -g window-status-current-format ' #I #W #F '
#
# setw -g window-status-style 'fg=colour111'
# setw -g window-status-format ' #I #[fg=colour7]#W #[fg=colour4]#F '
#
# setw -g window-status-bell-style 'fg=colour2 bg=colour4 bold'

# messages
# set -g message-style 'fg=colour2 bg=colour0 bold'

# dracula settings
set -g @dracula-show-powerline true
set -g @dracula-show-flags true
set -g @dracula-show-left-icon session
set -g @dracula-show-left-sep 
set -g @dracula-show-left-color "gray pink"
set -g @dracula-plugins "cpu-usage ram-usage battery"
set -g @dracula-cpu-usage-colors "gray cyan"
set -g @dracula-ram-usage-colors "gray white"
set -g @dracula-battery-colors "gray cyan"
set -g @dracula-battery-label "BAT"
# set -g status-position top
# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

